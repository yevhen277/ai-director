# Director Vision Tool 项目描述

## 项目目标

这个仓库实现的是一个面向机械臂摄影系统的视觉检测后端。它的目标是让机械臂能够根据画面中的目标位置自动调整镜头，例如先对准某个人、从男主横移到女主、再去拍桌上的饭或其他物体。

当前项目主要负责“看见目标并输出结构化坐标”，机械臂控制本体和前端指认页面可以在这个后端基础上继续接入。

## 当前已实现能力

### 1. YOLO 物体检测

项目使用 Ultralytics YOLO 检测图片或摄像头画面中的物体，输出目标名称、置信度和方框坐标。

模型文件统一放在：

```text
models/
```

当前模型包括：

```text
models/yolo26s.pt
models/yoloe-26s-seg.pt
models/yolov8n.pt
```

默认 YOLO 模型由 `.env` 或 `.env.example` 中的 `YOLO_MODEL` 指定，当前默认是：

```text
YOLO_MODEL=models/yolo26s.pt
```

如果实际检测效果不理想，可以切换成：

```text
YOLO_MODEL=models/yolov8n.pt
```

### 2. 机械臂专用检测接口

机械臂需要的是简单稳定的数据结构：物品名字、左上角坐标、右下角坐标。

项目已提供专门接口：

```text
POST /robot/detect/image
```

返回格式示例：

```json
{
  "object_count": 1,
  "objects": [
    {
      "object_name": "person",
      "confidence": 0.94,
      "top_left": {"x": 2, "y": 1115},
      "bottom_right": {"x": 632, "y": 1703}
    }
  ]
}
```

机械臂可以直接读取：

```text
object_name
top_left.x
top_left.y
bottom_right.x
bottom_right.y
```

也可以传入 `target` 只返回某类目标，例如：

```powershell
curl -X POST http://127.0.0.1:8000/robot/detect/image -F "target=person" -F "file=@images/0001.jpg"
```

### 3. 目标对准/运镜辅助

项目提供 `/aim/image` 和 `/aim/camera`，可以根据目标框中心与画面中心的偏移，返回简单运镜命令：

```text
search
hold
pan_left
pan_right
tilt_up
tilt_down
```

这些接口适合后续机械臂控制层做自动居中。

### 4. 现场指认式人脸识别

项目已接入 InsightFace `buffalo_l`，用于“现场指认一次”的身份绑定。

设计流程是：

```text
1. 前端上传全景图
2. 后端检测所有人脸，返回 face_id 和脸框
3. 用户在前端点击某个人脸
4. 前端把 face_id 绑定为 male_lead / female_lead / 自定义身份
5. 后续画面中，后端识别该身份并返回脸框、相似度和偏移
```

相关接口：

```text
POST /faces/candidates
POST /faces/register
POST /faces/recognize
GET  /faces/identities
```

身份注册文件默认保存到：

```text
data/face_registry.json
```

注意：这不是提前注册演员库，而是拍摄现场临时绑定身份。

### 5. CLI 图片检测和标注输出

项目也提供命令行工具：

```text
vision_cli.py
```

可以直接检测图片，或输出带检测框的图片。标注图片统一输出到：

```text
images/output
```

示例：

```powershell
.\.venv\Scripts\python.exe .\vision_cli.py .\images\0001.jpg
```

如果只需要终端 JSON，不需要保存框选图，可以加 `--no-output`。

## 核心文件说明

```text
app/api.py
```

FastAPI 后端入口，定义 YOLO 检测、机械臂接口、目标对准接口和人脸识别接口。

```text
app/detector.py
```

YOLO 检测封装，负责加载模型、检测目标、计算目标中心和运镜偏移。

```text
app/face_recognition.py
```

InsightFace 人脸识别封装，负责检测候选脸、注册身份、相似度匹配和画人脸框。

```text
app/config.py
```

配置加载模块，会读取项目根目录 `.env`，并提供默认配置。

```text
app/labels.py
```

目标别名映射，例如把 `food`、`rice`、`meal` 映射到 YOLO 可识别的类别。

```text
vision_cli.py
```

命令行检测工具，适合本地快速验证模型和输出标注图。

```text
deploy/install_ubuntu.sh
deploy/director-vision.service
```

Ubuntu 部署脚本和 systemd 服务配置。

## 运行方式

### 本地安装

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

### 启动 API

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.api:app --host 127.0.0.1 --port 8000
```

健康检查：

```powershell
curl http://127.0.0.1:8000/health
```

## 端口建议

建议把前端、视觉后端、机械臂控制服务分开：

```text
前端网页：5173
视觉 API：8000
机械臂控制服务：9000
```

推荐架构：

```text
前端网页 -> 视觉/导演后端 -> 机械臂控制服务
```

前端用于现场指认、调试和展示框；机械臂读取后端输出的结构化坐标。

## Ubuntu 部署注意事项

可以使用：

```bash
bash deploy/install_ubuntu.sh
```

部署后服务运行在：

```text
http://Ubuntu机器IP:8000
```

需要特别注意：

1. 模型文件必须存在于 Ubuntu 的 `/opt/director-vision/models/` 下。
2. 如果之前部署过，`/opt/director-vision/.env` 不会被 `.env.example` 自动覆盖，需要手动修改。
3. InsightFace 第一次调用会下载 `buffalo_l` 模型到 `/home/director/.insightface/models/buffalo_l`。
4. Ubuntu 可能需要安装构建依赖：

```bash
sudo apt-get install -y build-essential python3-dev
```

5. 如果局域网访问不到 8000 端口，需要检查防火墙：

```bash
sudo ufw allow 8000/tcp
```

## 当前项目状态

当前仓库已经具备后端 MVP 能力：

```text
YOLO 物体检测
机械臂坐标接口
目标对准辅助
现场指认式人脸识别
图片标注输出
Ubuntu systemd 部署脚本
```

下一步建议优先做前端现场指认页面，让用户能在网页上点击人脸框并绑定 `male_lead`、`female_lead`。等视觉链路稳定后，再接真实机械臂控制。
